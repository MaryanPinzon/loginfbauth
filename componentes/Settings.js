import { View, Text } from 'react-native';
import { styles } from '../assets/styles/allstyles';

export default function Settings() {
    return (
        <View styles={styles.container}>
        <Text>Estamos en la pantalla de configuración</Text>
        </View>
    );
}